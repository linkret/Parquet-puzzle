# Local dev (emulator)
# FIRESTORE_EMULATOR_HOST=127.0.0.1:8088

# Cloud project (optional locally; Cloud Run detects automatically)
GOOGLE_CLOUD_PROJECT=parquet-puzzle

# Non-default Firestore database (set on Cloud Run; optional locally)
FIRESTORE_DATABASE_ID=daily-puzzle

# Local-only: path to your service account JSON (do NOT commit your key .json!)
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\your-service-account.json